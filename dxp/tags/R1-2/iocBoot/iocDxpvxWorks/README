Files in this directory:

README - This file.

16element.cmd - An example vxWorks script, typically called from st.cmd 
with "< 16element.cmd".  This is for the 16 element detector at GSECARS.

16element.template - An example database template file that loads the DXP
and MCA records for the GSECARS 16 element detector.  Loaded from 16element.cmd.

d2xr0106.hex - The DSP microcode for the DXP-2X.  Downloaded to the
hardware by the dxp_initialize command in 16element.cmd.

f01x2pNg.fip - The FiPPI microcode for decimation N (where N=0,2,4,6).
Downloaded to the hardware by the dxp_initialize command in 16element.cmd.

dxp2x.cfg. - Default parameters for the DXP-2X.  These values will typically
be overwritten by values from save_restore.

dxp.module - Module configuration file that specifies the number of DXP modules
and channels in the system, the FiPPI microcode file to use (e.g. f01x2p2g.fip),
the DSP microcode file to use (e.g. d2xr0106.hex), and the default parameter file
(e.g. dxp2x.cfg) to use.

xiasystem.cfg - System configuration file that specifies the module configuration file
(e.g. dxp.module) to use.
